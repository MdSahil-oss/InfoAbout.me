let Server = {
    FrontEndPort : 3000,     // Change it as per your need
    BackEndPort : 4000,      // Change it as per your need
    APIsEndpoint : 'http://127.0.0.1:4000',       // Change it as per your need
    FrontEndPoint : 'http://localhost:3000',      // Change it as per your need
    ClientId : '',          // Insert Here ClientId of Twitter 
    ClientSecret : '',      // Insert Here ClientSecret of Twitter 
}

export default  Server